import 'package:flutter/material.dart';

const wht = Colors.white;
const blk = Colors.black;
const pink = Colors.pink;
const gry = Colors.grey;
const green = Colors.green;
const lightblk = Color.fromARGB(255, 50, 49, 49);
const dblk = Color.fromARGB(255, 40, 40, 40);
final themePurple = rgba(55, 0, 60, 1);
rgba(int r, int g, int b, double opacity) {
  return Color.fromRGBO(r, g, b, opacity);
}
